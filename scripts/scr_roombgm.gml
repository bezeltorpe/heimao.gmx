if (global.prevMapZone != global.currentMapZone)
    {audio_play_sound(BGM_Index, 1, true)}
    
else
    {
    //TODO: music channels turn on as the player progresses deeper into each zone,
    //      and turn off as they return closer to the nexus.
    //      toggle and xfade the appropriate channels depending on the map index.
    //      no other IDs are needed.
    //
    //      - rebecca
    
    //if (room == nexus) {sound_volume(mus_home, 0)}
    //if (room == home_int) {sound_volume(mus_home, 1)}
    }
